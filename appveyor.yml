environment:
  nodejs_version: "6.10.2"

# safelist
branches:
  only:
    - master

install:
  - npm install -g node-gyp
  - npm install -g gulp
  - npm install

build_script:
  - gulp buildDll --repoUrl="https://github.com/Microsoft/vscode-arduino.git" --token="5e1fd73eeb1640c5932c17994e9ad8ed0e6a1e9b" --tag="downloads"
  